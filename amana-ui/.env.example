# Copy to .env.local and adjust.
#
# Must match the network your Lace wallet is set to, or the connector refuses
# the connection. One of: preview | preprod | mainnet | undeployed
VITE_NETWORK_ID=preview

# trace | debug | info | warn | error
VITE_LOGGING_LEVEL=info
