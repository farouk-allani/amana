# dependencies
node_modules/

# build output
dist/
.vite/
*.tsbuildinfo

# Compact compiler output.
#
# Regenerated by `npm run compact`. The proving keys alone are ~32 MB, and they
# are a deterministic function of amana.compact and the compiler version — both
# of which are pinned and in the repository.
managed/

# local scratch
.smoke/
logs/
*.log

# Local Codex handoff/context. Keep project instructions on this machine only.
/AGENTS.md

# environment
.env
.env.local

# editors / os
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
